#CECS274 Lab Sec 07 Larry Delgado

import AdjacencyMatrix

class Node:
    
    
def main():
    pass

size = input("graph size: ")
alpha = []

graph = AjacencyMatrix.AdjacencyMatrix(size)

def interaction(self):
    pass

#graph with n nodes(peopple)
def getInteratctionGraph(self, n):
    g = AdjacencyMatrix.AdjacencyMatrix(n)

    for j in(n):
        i = rand.random(0:j))
        if i(0,1) < alpha:
            g.add_edge(j,i)
        else:
            k = rand.random(0:j)#random node that node i points
            g.add_edge(j,k)
        return g

def simulation(day)
    infected = 0
    dead = 0
    print(infected)
    print(dead)

    for v in(node):
        if infected:
            return None
    for w in range(v):
        if rand(0,1) < trasmission_rate:
            w.state. = infected
            w.recover = recovery_days
        v.recover = v.recover -1
        if v.recover == 0:
            if rand(0,1) < fatality_rate:
                v.state = dead
            else:
                v.state = recovered

def initialInfectedNode(self):
    for v in (node):
        if rand(0,1) < initial_setting:
            v.state = infected
            w.recover = recovery_days
        else:
            v.state = clean
